Libraries
=========

This file specifies what libraries are in this directory, what they are
used for, and when/where they are needed.  This way it is easy to
determine what to distribute.

   JAR NAME      VERSION  DESCRIPTION                    USED AT

antlr-2.7.5.jar   2.7.5   ANTLR lexer/parser         build + runtime
junit.jar         3.8.1   JUnit unit-test framework  build

